# No third-party dependencies - verify.py is stdlib-only (Python 3.11+).
# Stream verification is done by the external `ffprobe` binary (ffmpeg),
# which is a system tool, not a pip package.
#
# This file exists so `pip install -r requirements.txt` works in CI/venv
# setups and as the place to pin deps if any are added later (e.g. aiohttp).
